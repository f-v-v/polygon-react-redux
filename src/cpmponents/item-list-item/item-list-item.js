//Не исрользуется... Пока
import React from 'react';
import './itemListItem.css';

const itemListItem = ({name}) => {
    return (
        
            <span>{name}</span>

    )
}

export default itemListItem;

